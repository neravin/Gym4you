<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label "gymnasium:" %><br>
  <%= collection_select(:price, :gymnasium_id, Gymnasium.all, :id, :title, prompt: true) %><br /><br />

  <div class="field">
    <%= f.label :number_months %><br>
    <%= f.text_field :number_months %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :terms %><br>
    <%= f.text_area :terms %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
